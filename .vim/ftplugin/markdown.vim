" タブではなくスペースでインデントします。
setlocal expandtab

" インデント幅は 4 です。
setlocal shiftwidth=4
setlocal softtabstop=4
setlocal tabstop=4

" 自動的に折り畳まないようにします。
let vim_markdown_folding_disabled=1

" Markdown 形式のテーブルを自動整形します。
let table_mode_corner='|'
